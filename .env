# Google Cloud Project Configuration
PROJECT_ID=virtual-try-on-1
LOCATION=us-central1

# Authentication
# Only the service account key file (service-account-key.json) is used for authentication.
# Place your service-account-key.json file in the project root directory.

# ---------------------------------------------------------------------------
# PostgreSQL — single DATABASE_URL (Neon, Render Postgres, Railway, etc.)
# ---------------------------------------------------------------------------
# Neon: Dashboard → project → Connection string → copy (include ?sslmode=require).
# Hosted app (e.g. Render): set the same DATABASE_URL in Web Service → Environment.
#
DATABASE_URL=postgresql://neondb_owner:npg_XPH5JRCKcM8F@ep-proud-river-am39lvhb.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require

# MySQL (only used if DATABASE_URL is not set)
# DB_HOST=localhost
# DB_USER=
# DB_PASSWORD=
# DB_NAME=

# Session secret (change in production)
SESSION_SECRET=zaha-ai-session-secret-change-in-production
