# Google Cloud Project Configuration
PROJECT_ID=virtual-try-on-1
LOCATION=us-central1

# Authentication
# Only the service account key file (service-account-key.json) is used for authentication.
# Place your service-account-key.json file in the project root directory.

# PostgreSQL (Render) - when set, app uses Postgres and ignores DB_* below
# Use External URL for local dev; on Render set DATABASE_URL to the Internal Database URL in the service env.
DATABASE_URL=postgresql://style_nova:ytsnnURXPPxaUaiWZIOCmonxsK7S33od@dpg-d6c0idogjchc73fvp5t0-a.oregon-postgres.render.com/style_nova?sslmode=require

# MySQL (only used if DATABASE_URL is not set)
# DB_HOST=localhost
# DB_USER=
# DB_PASSWORD=
# DB_NAME=

# Session secret (change in production)
SESSION_SECRET=zaha-ai-session-secret-change-in-production

